This directory snapshots the local `.omc/state` files that existed inside
`vendor/AzurLaneAutoScript` on 2026-03-19 when the branch was reconciled.

These files were untracked inside the ALAS submodule, so they would not have
been preserved by normal superproject commits. They are copied here only to
avoid losing local state during branch cleanup.
